{
  "name": "movie-api-proxy",
  "version": "1.0.0",
  "description": "Azure Function proxy for TMDB API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "typescript": "^5.9.3"
  }
}